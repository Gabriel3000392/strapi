'use strict';

/**
 * sponsors-or-partners service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sponsors-or-partners.sponsors-or-partners');
