'use strict';

/**
 * sponsors-or-partners router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::sponsors-or-partners.sponsors-or-partners');
